import 'package:flutter/cupertino.dart';

Gradient design = const LinearGradient(
  colors: [Color.fromRGBO(0, 242, 96, 1), Color.fromRGBO(5, 117, 230, 1)],
  begin: Alignment.topRight,
  end: Alignment.bottomLeft,
);

Gradient code = const LinearGradient(
  colors: [Color.fromRGBO(106, 48, 147, 1), Color.fromRGBO(160, 68, 255, 1)],
  begin: Alignment.bottomRight,
  end: Alignment.bottomLeft,
);

Gradient ship = const LinearGradient(
  colors: [Color.fromRGBO(111, 177, 252, 1), Color.fromRGBO(0, 82, 212, 1)],
  begin: Alignment.bottomRight,
  end: Alignment.topLeft,
);

Gradient black = const LinearGradient(
  colors: [Color.fromRGBO(0, 0, 0, 1), Color.fromRGBO(0, 0, 0, 1)],
  begin: Alignment.bottomRight,
  end: Alignment.topLeft,
);
